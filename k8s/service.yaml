apiVersion: v1
kind: Service
metadata:
  name: insure-me
  labels:
    app: insure-me
spec:
  type: NodePort
  selector:
    app: insure-me
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 30082
